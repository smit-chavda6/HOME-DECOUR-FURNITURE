// Placeholder gallery data to prevent 404 and MIME errors on pages loading this script.
// If needed, populate with real gallery items later.
// Example shape: [{ id, name, price, image, category }]
window.GALLERY_DATA = window.GALLERY_DATA || [];
